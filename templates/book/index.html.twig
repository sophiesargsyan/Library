{% extends 'base.html.twig' %}

{% block title %}Գրքերի ցուցակ{% endblock %}

{% block body %}
    <h1>📚 Գրքերի ցուցակ</h1>

    <a href="{{ path('book_new') }}">➕ Ավելացնել նոր գիրք</a>

    <ul>
        {% for book in books %}
            <li>
                <strong>{{ book.title }}</strong> — {{ book.author }} ({{ book.year }})
                <br>
                <em>{{ book.description|length > 100 ? book.description[:100] ~ '...' : book.description }}</em>
                {% if book.coverImage %}
                    <img src="{{ asset('uploads/covers/' ~ book.coverImage) }}" alt="{{ book.title }} Cover" width="150">
                {% endif %}
            </li>
        {% else %}
            <li>Գրքեր չկան։</li>
        {% endfor %}
    </ul>
{% endblock %}